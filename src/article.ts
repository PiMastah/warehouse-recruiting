class Article {
  id: number
  name: String
  stock: number

  constructor(id: number, name: String, stock) {
    this.id = id
    this.name = name
    this.stock = stock
  }
}

export default Article